<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:windowNoTitle">true</item>
    </style>
	<style name="dialog_popupwindow_anim">
	    <item name="android:windowEnterAnimation">@anim/poupwindow_show</item>
	    <item name="android:windowExitAnimation">@anim/popupwindow_exit</item>
	</style>
	<!--    android:layout_width="0dp"
            android:layout_weight="1"
            android:gravity="center_horizontal"
            android:layout_height="wrap_content"
            android:textColor="#0A0A0A"
            android:textSize="18sp" -->
    <!-- 对话框中时，分，秒的样式 -->
    <style name="dialog_text">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">#0A0A0A</item>
    </style>
    <!--    android:layout_width="0dp"
            android:layout_weight="1"
            android:id="@+id/et_dialog_hour"
            android:inputType="number"
            android:maxLength="2"
            android:layout_height="wrap_content"
            android:gravity="center_horizontal"
            android:textColor="#9F79EE" -->
     <!-- 对话框中edittext的文字属性 -->
     <style name="dialog_edittext">
         <item name="android:layout_width">0dp</item>
         <item name="android:layout_weight">1</item>
         <item name="android:inputType">number</item>
         <item name="android:maxLength">2</item>
         <item name="android:layout_height">wrap_content</item>
         <item name="android:gravity">center_horizontal</item>
         <item name="android:textColor">#9F79EE</item>
         <item name="android:textSize">18sp</item>
     </style>
     <!-- android:layout_width="wrap_content"
          android:layout_height="wrap_content"
          android:id="@+id/sp_history_pollution"
          android:background="@drawable/spinner_arrow_background"
          android:popupBackground="@drawable/spinner_background"
          android:spinnerMode="dropdown"  
          android:dropDownVerticalOffset="3dp"  
          android:dropDownWidth="wrap_content"
          android:entries="@array/sp_history_arrays" -->
          <!-- spinner的样式 -->
          <style name="spinner_popudown_style">
              <item name="android:layout_width">wrap_content</item>
              <item name="android:layout_height">match_parent</item>
              <item name="android:popupBackground">@drawable/spinner_background</item> 
              <item name="android:spinnerMode">dropdown</item>
              <item name="android:dropDownVerticalOffset">3dp</item>
              <item name="android:dropDownWidth">wrap_content</item>
              <item name="android:background">@null</item>
              <item name="android:gravity">center</item>
              
          </style>
</resources>
